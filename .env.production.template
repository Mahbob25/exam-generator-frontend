# Production Environment Variables Template
# Add these to your Vercel project settings

# REQUIRED: Backend API URL (your Cloud Run URL)
NEXT_PUBLIC_API_URL=https://your-backend-service.run.app

# REQUIRED: API Key for backend authentication
# This is used on the server-side to authenticate with the backend
BACKEND_API_KEY=your_api_key_here

# Optional: Environment
NEXT_PUBLIC_ENVIRONMENT=production

# Note: BACKEND_API_KEY is preferred over NEXT_PUBLIC_API_KEY for security
# effectively keeping it hidden from the client browser when used in Server Actions/Components
